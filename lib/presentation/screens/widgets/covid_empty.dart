import 'package:flutter/material.dart';

class CovidEmpty extends StatelessWidget {
  const CovidEmpty({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
    );
  }
}